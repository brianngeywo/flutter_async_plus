name: async_o_loader
description: Handle async tasks with ease through automatic loading, error handling, and native widget integration.
version: 0.14.0
topics:
 - async
 - implicitly
 - implicit
 - automatic
 - state

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  async_notifier: ^0.5.0
  flutter:
    sdk: flutter
  hexcolor: ^3.0.1
  loading_indicator: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: